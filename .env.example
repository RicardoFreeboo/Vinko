# Vinko alfa — copiar a .env.local (y a las variables de entorno del sitio en Netlify).
# JAMÁS commitear claves reales; la service_role de Supabase no sale del servidor.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

SENTRY_DSN=

# Flags del ALPHA FREEZE (CLAUDE.md)
# LEGAL_LOCK=true → staging no público salvo /p/[slug] de plantillas para probar OG;
# botón rewarded oculto. Solo Ricardo lo quita.
LEGAL_LOCK=true
# ADS_ENABLED=false → stub de rewarded (overlay de test) y display no-op.
ADS_ENABLED=false
